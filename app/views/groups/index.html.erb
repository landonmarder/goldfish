<% if current_user %>
  <h1>My Groups</h1>

  <table class="table table-hover">
    <thead>
      <tr>
        <th>Title</th>
        <th># of Members</th>
        <th>Pending Activites</th>
        <th>Activities Completed</th>
        <th>Percent Activities</th>
      </tr>
    </thead>
    <tbody>
      <% @groups.each do |group| %>
        <tr>
          <th><%= link_to group.title, group_path(group) %></th>
          <th><%= group.memberships.count %></th>
          <th>8</th>
          <th>12</th>
          <th>60%</th>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
<h1>Home Page</h1>
<% end %>
